function getNewTaskInfo() {
  const title = "this task should end up in the inbox";
  const description = "it's generated by the input-collector module";
  const dueDate = "";
  const priority = "medium";

  const details = [title, description, dueDate, priority];
  return details;
}

const taskInfo = getNewTaskInfo();
export default taskInfo